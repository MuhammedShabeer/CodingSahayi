<ContentDialog
    x:Class="CodingSahayi.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    Title="Settings"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <ScrollViewer MaxHeight="500">
        <StackPanel Spacing="8" MinWidth="450">
            <toolkit:SettingsCard Header="API Key" Description="Your NVIDIA NIM or OpenAI token">
                <PasswordBox x:Name="ApiKeyBox" Width="250" />
            </toolkit:SettingsCard>
            
            <toolkit:SettingsCard Header="API Base URL" Description="The OpenAI-compatible endpoint">
                <TextBox x:Name="EndpointBox" Width="250" />
            </toolkit:SettingsCard>
            
            <toolkit:SettingsCard Header="Model Name" Description="The LLM to use for vibe coding">
                <TextBox x:Name="ModelNameBox" Width="250" />
            </toolkit:SettingsCard>
            
            <toolkit:SettingsExpander Header="System Prompt" Description="Agent rules and operating context" IsExpanded="True">
                <toolkit:SettingsExpander.Items>
                    <toolkit:SettingsCard ContentAlignment="Left">
                        <TextBox x:Name="SystemPromptBox" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="150" HorizontalAlignment="Stretch" />
                    </toolkit:SettingsCard>
                </toolkit:SettingsExpander.Items>
            </toolkit:SettingsExpander>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
